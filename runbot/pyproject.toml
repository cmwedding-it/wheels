[project]
name = "odoo-addons-runbot"
version = "%s"

[build-system]
requires = ["hatchling", "hatch-odoo"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["runbot_builder"]

[tool.hatch.build.hooks.odoo-addons-dirs]

[tool.hatch-odoo]
addons_dirs = ["."]
